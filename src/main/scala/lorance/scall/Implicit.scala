package lorance.scall

/**
  * all of the implicit method
  */
object Implicit {
//  implicit def toCmd(cmd: String) = Cmd(cmd)
//
//  implicit def toCmdFlow(cmd: String) = CmdFlow(Cmd(cmd))
//  implicit def cmdToCmdFlow(cmd: Cmd) = CmdFlow(cmd)
//
//  implicit def toContextCmdFlow(cmd: String) = ContextCmdFlow(_ => CmdFlow(Cmd(cmd)))
//  implicit def toContextCmdFlow(cmd: CmdFlow) = ContextCmdFlow(_ => cmd)
////  implicit def toContextCmdFlow(contextCmd: Option[String] => String) = ContextCmdFlow(contextOpt => CmdFlow(Cmd(contextCmd(contextOpt))))
//  implicit def toContextCmdFlow(contextCmd: Option[String] => CmdFlow) = ContextCmdFlow(contextOpt => contextCmd(contextOpt))

}
